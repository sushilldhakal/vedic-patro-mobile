import type { LocationParams } from "@/lib/api";

type Props = {
  dateAd: string;
  location: LocationParams;
};

/** Native stub — gochar API not wired on mobile yet. */
export function PlanetEventsPanel(_props: Props) {
  return null;
}
